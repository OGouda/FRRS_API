setup:
build:
  docker:
    web: Dockerfile
    worker: worker/Dockerfile
  config:
    APP_SETTINGS: config.DevelopmentConfig
release:
  command:
    - ./First_Setup.sh
  image: worker
run:
  web: python app.py
  worker: python myworker.py